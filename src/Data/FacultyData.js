// import { onValue, ref } from 'firebase/database'
// import { database } from '../JS/Firebase'
// export let facultyList = []

// onValue(ref(database, `users`), user => {
//     if (user.exists()) {
//         facultyList = Object.values(user.val())
//     }
// })